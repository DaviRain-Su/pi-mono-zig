{
	"schemaVersion": "pi-extension.v0",
	"id": "com.pi.browser-fixture.filesystem-request",
	"name": "Browser Filesystem Request Fixture",
	"version": "0.1.0",
	"description": "Fixture that requests local filesystem capability for browser host denial validation.",
	"artifact": {
		"kind": "wasm-component",
		"path": "wasm/plugin.wasm"
	},
	"tool": {
		"id": "fixture.filesystem-request",
		"description": "Requests local filesystem access that the browser host must deny.",
		"inputSchema": {},
		"outputSchema": {}
	},
	"capabilities": ["file.read"]
}
