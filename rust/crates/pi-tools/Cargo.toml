[package]
name = "pi-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
pi-zig-codegen = { path = "../pi-zig-codegen" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
