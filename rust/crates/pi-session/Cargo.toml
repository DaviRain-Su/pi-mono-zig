[package]
name = "pi-session"
version = "0.1.0"
edition = "2021"

[dependencies]
pi-ai = { path = "../pi-ai" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
