[package]
name = "pi-zig"
version = "0.1.0"
edition = "2021"

[dependencies]
pi-zig-sys = { path = "../pi-zig-sys" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
